# レビューガイドライン

## 言語

すべてのレビューコメント・要約・バグ報告・フラグは **日本語** で記述してください。

## 重点チェック領域

- `src/WindowsTetris/Game/` 配下のゲームロジック変更は、既存のテトリス仕様（回転・衝突判定・ライン消去・スコア計算）を壊さないか注意してレビューすること。
- ステージモードのレイアウトデータ (`Stages.cs`) を変更する場合は、Gem の配置が正しいか確認すること。
- `MainWindow.xaml` / `MainWindow.xaml.cs` の UI 変更は、WPF の Fluent Dark テーマとの整合性を確認すること。

## コーディング規約

- C# / .NET 10 のコーディング規約に準拠すること。
- `public` メソッドには XML ドキュメントコメントを推奨する。
- マジックナンバーは定数として定義し、意味のある名前を付けること。
- XAML と C# のコードビハインドの責務を明確に分離すること。

## 無視してよいもの

- `Assets/Audio/` 配下のバイナリファイルの変更はレビュー不要。
- 自動生成ファイル (`obj/`, `bin/`) はレビュー不要。

## パフォーマンス

- ゲームループ内（`DispatcherTimer` コールバック等）で不要なオブジェクト生成やアロケーションがないか確認すること。
- XAML レンダリングに影響するバインディングの変更には注意すること。
